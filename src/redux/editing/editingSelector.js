const getIsEditing = state => state.editing.isEditing;
const getIdEditing = state => state.editing.id;
const getNameEditing = state => state.editing.name;
const getNumberEditing = state => state.editing.number;

// eslint-disable-next-line
export default {
  getIsEditing,
  getIdEditing,
  getNameEditing,
  getNumberEditing,
};
